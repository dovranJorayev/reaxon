# Package-manager behaviour that must be identical for every contributor and CI.
save-exact=true
engine-strict=true
# Publishing is done by CI through npm trusted publishing (OIDC). No token lives here.
# For the one-time bootstrap publish see docs/reports/2026-09-14-github-ci-token-injection.md
